import hello # Import the hello module (assumes hello.py exists in the same directory)

# The following import statements are redundant and do nothing new,
# because Python only imports a module once per session
import hello
import hello
